Plan: 1 to add.

Summary by type:
  tables: 1 to add

Tables:
  + orders_us

DDL to be executed:
--------------------------------------------------

CREATE TABLE IF NOT EXISTS orders_us PARTITION OF orders (
    priority DEFAULT 10,
    notes NOT NULL
) FOR VALUES IN ('us');
